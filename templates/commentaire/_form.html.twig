

    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
    <div class="row">

        <div>
            <label for="inputText" class="col-sm-5 col-form-label">Commentaire</label>
        <div class="col form-group">
            {{ form_widget(form.contenu,{'attr':{'class':'form-control'}}) }}
            {{ form_errors(form.contenu,{'attr':{'class':'text-danger'}}) }}

        </div>
        </div>
    </div>

    <button type="submit" class="btn btn-primary"> {{ button_label|default('poster commentaire') }}</button>

    {{ form_end(form) }}


